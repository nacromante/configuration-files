server:
    port: 8090

spring:
    application:
        name: studants-service
eureka:
  client:
    service-url:
      defaultZone: http://${EUREKA_HOST:192.168.3.147}:${EUREKA_PORT:8761}/eureka

msg:
    error: mensagem final

