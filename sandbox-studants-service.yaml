server:
    port: 8090

spring:
    application:
        name: studants-service
eureka:
  client:
    service-url:
      defaultZone: http://${EUREKA_HOST:localhost}:${EUREKA_PORT:8761}/eureka

msg:
    error: teste de mensagem com ácento ô ão

